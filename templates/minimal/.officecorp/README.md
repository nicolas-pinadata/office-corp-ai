# Minimal OfficeCorp Install

Copy this `.officecorp/` folder into any project.

These files are enough for basic OfficeCorp routing:

- `config.yml`
- `current-state.md`
- `routing.yml`
- `token-budget.yml`

Optional files such as `project-brief.md`, `decisions/`, and `playbooks/` can be added later.

